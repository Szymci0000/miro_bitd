# Paste into Miro app settings → Edit in Manifest
# See https://developers.miro.com/docs/app-manifest
appName: Miro App
sdkVersion: SDK_V2
sdkUri: http://localhost:3000
scopes:
  - boards:read
  - boards:write
